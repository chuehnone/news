news.db
__pycache__/
*.pyc
fetch.log
dist/
# 本機筆記：專案的方向決策與操作前提。repo 是 public，
# 策略思考不需要對外揭露，故只留在本機。
NOTES.local.md
# 本機跨工具交接文件，包含未公開的商業方向。
HANDOFF.local.md
# 獨立的私有子專案（自帶 .git，不是 submodule），內容不屬於這個 public repo。
/commercial/

# API 憑證：key 一律放家目錄（~/.typesafe_key），不該出現在 repo 內。
# 這幾條是萬一有人為了方便把它複製進來時的防線——repo 是 public，
# 一旦 push 上去就等於公開，撤回也來不及（GitHub 會留在 fork 與快取裡）。
*.key
.typesafe_key
*_key.txt
.env
